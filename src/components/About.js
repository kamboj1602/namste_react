const About = () => {
  return (
    <main className="page-layout">
      <section className="page-panel">
        <h1>About Namste React</h1>
        <p>
          This example app demonstrates a responsive restaurant listing page
          built with React and React Router. Use the search box, filters, and
          navigation links to explore the interface.
        </p>
      </section>
    </main>
  );
};

export default About;
